// Vehicle Listing Tracker — extension config (EXAMPLE)
//
// Copy this file to `config.js` in the same folder and fill in your real
// Supabase project values. `config.js` is gitignored — never commit real keys.
//
// These are declared as top-level consts so the other content scripts
// (lib/supabase.js, etc.) can read them — all content scripts in this
// extension share one isolated-world scope per page.

const SUPABASE_URL = "YOUR_SUPABASE_URL";
const SUPABASE_ANON_KEY = "YOUR_SUPABASE_ANON_KEY";
